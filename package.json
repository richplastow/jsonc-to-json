{
  "name": "jsonc-to-json",
  "version": "0.0.1",
  "description": "Minimal transformer of JSONC to JSON, that strips comments and trailing commas",
  "keywords": [
    "jsonc",
    "json"
  ],
  "homepage": "https://richplastow.com/jsonc-to-json/",
  "bugs": {
    "url": "https://github.com/richplastow/jsonc-to-json/issues"
  },
  "license": "MIT",
  "author": "Rich Plastow",
  "main": "./dist/jsonc-to-json.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/richplastow/jsonc-to-json.git"
  },
  "scripts": {
    "build": "rollup -c && echo '\\xE2\\x9C\\x85 Build succeeded!'",
    "check-types": "tsc --noEmit && echo '\\xE2\\x9C\\x85 No type-errors found!'",
    "prebuild": "cd wasm/jsonc_to_json && wasm-pack build --target web --out-dir pkg && cd ../.. && node scripts/embed-wasm.js",
    "ok": "npm run test:js && npm run check-types && npm run build && npm run test:wasm",
    "test:js": "node test.js && echo '\\xE2\\x9C\\x85 All JS tests passed!'",
    "test:wasm": "cd wasm/jsonc_to_json && cargo test --release && echo '\\xE2\\x9C\\x85 All WASM tests passed!'",
    "test": "npm run test:js && npm run test:wasm"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.8.1"
  },
  "engines": {
    "node": "24.8"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.9.2",
  "type": "module"
}
